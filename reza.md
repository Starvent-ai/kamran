# Starvent — مستندات فنی کامل پروژه

این فایل باید همیشه با کد هماهنگ باشد. **هر تغییری که در پروژه اعمال می‌شود، باید همان لحظه
در بخش مربوطهٔ همین فایل هم ثبت شود** — این فایل تنها منبع مرجع کامل پروژه است و باید به‌تنهایی
(بدون نیاز به خواندن کد یا توضیح اضافی) کل منطق، معماری و وضعیت فعلی برنامه را قابل‌فهم کند.

---

## ۱. معرفی کلی

**Starvent** یک نرم‌افزار دسکتاپ مدیریت فروشگاهی برای فروشگاه‌های موبایل‌فروشی و تعمیرات موبایل
است. نسخهٔ فعلی مخصوص ویندوز است. کاربر هدف: صاحب یا کارکنان یک فروشگاه موبایل که نیاز به مدیریت
فروش، انبار، تعمیرات، مشتریان، فروش اقساطی، ضمانت، حسابداری و پیامک/اطلاع‌رسانی دارند.

رابط کاربری کاملاً فارسی و راست‌چین است. طراحی بصری: مینیمال، حرفه‌ای، تم طلایی/تیره روی
پس‌زمینهٔ کرم روشن با جلوهٔ شیشه‌ای مات (glassmorphism) روی پنجره‌ها.

---

## ۲. محیط فنی و زبان برنامه‌نویسی

| بخش | فناوری |
|---|---|
| زبان اصلی | TypeScript (سخت‌گیرانه — `strict: true`) |
| فریم‌ورک UI | React 18 (فقط با `useSyncExternalStore` بومی — بدون Redux/Zustand/MobX) |
| پلتفرم دسکتاپ | Electron 31 |
| باندلر رندرر | Vite |
| بستهٔ نصبی | electron-builder (خروجی: Installer NSIS، Portable، ZIP) — فقط ویندوز |
| فونت | Vazirmatn (پکیج npm رسمی، بدون فونت‌های تجاری) |
| تست | Vitest (محیط jsdom) |
| ذخیره‌سازی روی دیسک | electron-store (رمزنگاری‌شده، فایل JSON) |
| به‌روزرسانی خودکار | electron-updater (در برابر GitHub Releases) |
| اسکرِیپینگ وب (قیمت لحظه‌ای) | cheerio |
| ماژول سیستم بسته (`"type": "module"` در package.json) | ESM واقعی Node — importهای نسبی در `src/main/` نیازمند پسوند صریح `.js` هستند |

نسخهٔ Node مورد استفاده در CI: 20.
نسخهٔ TypeScript پروژه: `^5.5.3`.

### چرا این پشته؟
- بدون کتابخانهٔ state-management خارجی: حجم باندل کمتر و منطق ساده‌تر برای یک اپ به این
  اندازه؛ `useSyncExternalStore` React برای این مقیاس کافی است.
- بدون native dependency سنگین (مثل `better-sqlite3` یا رمزنگاری native): چون CI (GitHub
  Actions) روی رانر استاندارد با رم محدود (۷ گیگ) اجرا می‌شود و کامپایل ماژول native می‌تواند
  build را ناموفق کند یا کند کند.

---

## ۳. معماری کلی — Plugin-Based

هستهٔ برنامه (`src/renderer/App.tsx` و `src/renderer/components/Sidebar.tsx`) هیچ ماژولی را
مستقیماً import نمی‌کند. یک رجیستری مرکزی (`src/renderer/plugins/pluginRegistry.ts`) این کار را
انجام می‌دهد:

```ts
export interface StarventPlugin {
  id: string;              // شناسهٔ یکتا، انگلیسی
  label: string;           // برچسب فارسی در نوار کناری
  icon: string;            // یک نویسهٔ ساده (بدون فونت آیکون خارجی)
  order: number;           // ترتیب داخل گروه خودش
  group?: string;          // گروه‌بندی در نوار کناری
  essential?: boolean;     // اگر true، هرگز از افزونه‌ها غیرفعال نمی‌شود
  component: ComponentType;
}
```

هر ماژول در فایل `index.ts` خودش، خودش را با یک فراخوانی `pluginRegistry.register(...)` ثبت
می‌کند. تنها فایلی که برای افزودن ماژول جدید ویرایش می‌شود
`src/renderer/plugins/loadCoreModules.ts` است (یک خط `import`).

راهنمای کامل ساخت ماژول جدید (الگو، نمونه، قواعد) در فایل `PLUGIN_SDK.md` است — آن فایل هم باید
با هر تغییر معماری به‌روز بماند.

### افزونه‌های موجود (۱۶ ماژول، با شناسه و گروه)

| گروه | ماژول‌ها (id) |
|---|---|
| عملیات روزانه | `dashboard`، `sales`، `repairs`، `customers` |
| کالا و انبار | `inventory`، `live-prices`، `warehouse` |
| مالی | `installments`، `collateral`، `suppliers`، `accounting`، `calculator` |
| ابزارها | `printing`، `notifications` |
| سیستم | `security`، `settings` |

(`ai-suggestions-example` نمونهٔ یک افزونهٔ کاملاً مستقل خارج از پوشهٔ `modules/` است، در
`src/renderer/plugins/examples/`.)

### مدیریت فعال/غیرفعال‌سازی افزونه‌ها
`src/renderer/state/pluginPreferences.ts` — یک استور با کلید پایدار `data-plugin-preferences`
که فهرست شناسه‌های غیرفعال‌شده را نگه می‌دارد. ماژول‌های `essential: true` (`dashboard`،
`settings`، `security`) هرگز از این طریق غیرفعال نمی‌شوند. رابط کاربری‌اش در
تنظیمات → افزونه‌ها است.

---

## ۴. مدیریت وضعیت (State) و لایهٔ ذخیره‌سازی

### `createStore` — هستهٔ مشترک همهٔ استورها
مسیر: `src/renderer/state/createStore.ts`.

```ts
export function createStore<T>(initialState: T, persistKey?: string): {
  getState: () => T;
  setState: (updater: (prev: T) => T) => void;
  useStore: () => T; // هوک React، مبتنی بر useSyncExternalStore
}
```

- بدون `persistKey`: کاملاً درون‌حافظه‌ای و موقتی (برای وضعیت‌های صرفاً جلسه‌ای مثل ناوبری یا
  «درخواست چاپ در انتظار»).
- با `persistKey`: در لحظهٔ ساخت، یک بار به‌صورت async مقدار قبلاً ذخیره‌شده را می‌خواند
  (`loadPersisted`) و اگر چیزی پیدا شد، state اولیه (seed) را با آن جایگزین می‌کند. هر فراخوانی
  `setState` یک نوشتن روی دیسک را با تأخیر ۴۰۰ میلی‌ثانیه (debounce) زمان‌بندی می‌کند
  (`schedulePersist`) — این نوشتن همیشه **مقدار لحظهٔ واقعی اجرا** را می‌خواند و ذخیره می‌کند، نه
  یک snapshot قدیمی؛ این طراحی از رونویسی داده‌های واقعی دیسک با state موقت جلوگیری می‌کند و در
  عین حال تضمین می‌کند هیچ تغییری گم نشود، حتی اگر بلافاصله بعد از ساخت استور (پیش از پایان
  خواندن اولیه) رخ داده باشد.

### کانال فیزیکی ذخیره‌سازی
- در Electron واقعی: از طریق `window.starvent.settings.get/set` (پل preload) که در پردازش
  اصلی به `electron-store` وصل است — یک فایل JSON رمزنگاری‌شده روی دیسک کاربر.
- در محیط توسعه (Vite dev server) یا تست (Vitest/jsdom)، که پل preload وجود ندارد: به
  `localStorage` مرورگر سقوط می‌کند (fallback).

⚠️ **این یک پایگاه‌دادهٔ رابطه‌ای واقعی (مثل SQLite) نیست.** هر بار نوشتن، کل آرایهٔ مربوط به
همان کلید را به‌صورت یک بلاک JSON کامل بازنویسی می‌کند. برای مقیاس یک فروشگاه معمولی (چند صد تا
چند هزار رکورد در سال) کافی است؛ در مقیاس بسیار بزرگ (ده‌ها هزار رکورد در یک فایل) ممکن است کند
شود. مهاجرت به یک پایگاه‌دادهٔ واقعی (مثلاً SQLite) در آینده، تنها نیازمند تغییر پیاده‌سازی داخلی
`createStore.ts` است — چون همهٔ ماژول‌ها فقط از طریق این یک انتزاع به داده دسترسی دارند.

### فهرست کامل کلیدهای ذخیره‌سازی (persistKey) در حال حاضر

| کلید | ماژول | محتوا |
|---|---|---|
| `data-sales` | sales | رکوردهای فروش |
| `data-inventory` | inventory | کالاهای انبار مرکزی |
| `data-customers` | customers | مشتریان |
| `data-repairs` | repairs | پرونده‌های تعمیر |
| `data-suppliers` | suppliers | تأمین‌کنندگان و خریدها |
| `data-accounting` | accounting | تراکنش‌های مالی و چک‌ها |
| `data-installments` | installments | شرکت‌ها، قراردادها، پرداخت‌های اقساط |
| `data-collateral` | collateral | رکوردهای ضمانت |
| `data-warehouse` | warehouse | انبارها، موجودی غیرمرکزی، انتقال، رزرو، معیوب، مرجوعی |
| `data-users` | security | فقط فهرست کاربران (نه وضعیت لاگین — عمداً persist نمی‌شود) |
| `data-activity-log` | security | لاگ فعالیت‌ها |
| `data-sms-log` | notifications | تاریخچهٔ پیامک‌های ارسالی |
| `data-sms-event-queue` | notifications | صف پیامک‌های در انتظار تأیید |
| `data-sms-templates` | notifications | قالب‌های پیامک |
| `data-incoming-captures` | notifications | شماره‌های دریافتی از دستگاه ثبت‌شماره |
| `data-plugin-preferences` | (سیستمی) | افزونه‌های غیرفعال‌شده |
| `store-profile` | (سیستمی) | تنظیمات فروشگاه (نام، لوگو، پرینتر، بکاپ، محاسبات، اقساط) |
| `ai-provider-config` | settings | تنظیمات ارائه‌دهندهٔ هوش مصنوعی |
| `mobile-price-source` / `mobile-price-list` | live-prices | تنظیمات و آخرین نتیجهٔ اسکرِیپ قیمت |

استورهای بدون persistKey (عمداً موقتی): `navigationStore` (تب فعال نوار کناری)،
`printRequestStore` (درخواست چاپ در انتظار برای فروش/رزرو)، `customerIntakeStore` (شمارهٔ در
انتظار هنگام ثبت مشتری جدید)، و `sessionStore` داخل `useUsers.ts` (کاربر لاگین‌شدهٔ فعلی —
عمداً هر بار اجرای برنامه ریست می‌شود تا نیاز به ورود مجدد باشد).

---

## ۵. مدل امنیت و دسترسی (RBAC)

مسیر: `src/renderer/modules/security/useUsers.ts`.

- رمز عبور با SHA-256 (بدون salt) هش می‌شود؛ خودِ رمز هرگز ذخیره یا لاگ نمی‌شود.
- تا وقتی هیچ کاربری ساخته نشده، برنامه کاملاً باز است (بدون صفحهٔ ورود) — این‌طوری نصب تازه هیچ‌وقت
  قفل نمی‌شود.
- به‌محض ساخته شدن اولین کاربر، `App.tsx` یک گیت ورود واقعی نمایش می‌دهد
  (`src/renderer/components/LoginScreen.tsx`) تا زمانی‌که یک کاربر با موفقیت لاگین کند.
- نگاشت نقش → دسترسی (`ROLE_ACCESS`، ثابت است، در کد تعریف شده):

```ts
مدیر:      "all"  // دسترسی کامل به همهٔ ماژول‌ها
صندوقدار: ["dashboard", "sales", "customers", "calculator", "printing"]
تکنسین:    ["dashboard", "repairs", "printing"]
انباردار:  ["dashboard", "inventory", "warehouse", "suppliers"]
```

- `App.tsx` بر اساس نقش کاربر لاگین‌شده، نوار کناری را فیلتر می‌کند — ماژولی که نقش کاربر اجازه
  ندارد اصلاً در سایدبار دیده نمی‌شود.
- دکمهٔ خروج در نوار بالای صفحه (`TopBar.tsx`) همیشه در دسترس است، صرف‌نظر از نقش.

---

## ۶. ساختار پوشه‌ها

```
src/
  main/                 پردازش اصلی Electron (Node، ESM — importهای نسبی با پسوند .js)
    index.ts            نقطهٔ ورود، ساخت پنجره‌ها، ثبت IPC handlerهای settingsStore
    preload.cjs          پل contextBridge بین main و renderer (CommonJS)
    backup.ts / backupCore.ts   منطق بکاپ واقعی (گرفتن/فهرست/بازیابی/حذف)
    update.ts            منطق به‌روزرسانی خودکار (electron-updater)
    mobilePrices.ts       اسکرِیپینگ قیمت لحظه‌ای (cheerio)
    sms.ts / smsSend.ts    اتصال به دستگاه ثبت‌شماره و پنل پیامکی REST
    phoneCapture.ts        دریافت شمارهٔ ورودی از دستگاه سخت‌افزاری
  renderer/
    App.tsx               پوستهٔ اصلی برنامه — گیت ورود + فیلتر RBAC + رندر ماژول فعال
    main.tsx               نقطهٔ ورود React
    global.d.ts             تایپ کامل window.starvent (پل preload)
    components/            کامپوننت‌های مشترک (Sidebar، TopBar، CurrencyInput، LoginScreen، ...)
    state/                 استورهای سراسری غیرمتصل به یک ماژول خاص (navigation، print request، ...)
    lib/                   توابع خالص مشترک (id.ts، jalali.ts، hash.ts، storeProfile.ts، ...)
    plugins/
      pluginRegistry.ts      تعریف و پیاده‌سازی رجیستری
      loadCoreModules.ts      فهرست import همهٔ ماژول‌های فعلی
      examples/                نمونه افزونهٔ مستقل
    modules/<نام‌ماژول>/
      index.ts              ثبت در pluginRegistry
      use<ماژول>.ts           منطق state + عملیات (actions) + خروجی هوک React
      <ماژول>.tsx             کامپوننت UI
  shared/
    types.ts               همهٔ تایپ‌های مشترک بین main و renderer (منبع واحد حقیقت برای شکل داده)
tests/                     تست‌های Vitest (یک فایل معمولاً هم‌نام با ماژول/فایل موردنظر)
.github/workflows/build.yml   پایپ‌لاین CI/CD
```

---

## ۷. الگوریتم‌ها و منطق‌های غیربدیهی

### تبدیل تاریخ شمسی/میلادی — `src/renderer/lib/jalali.ts`
پیاده‌سازی دستی (بدون کتابخانهٔ خارجی) الگوریتم تقویم جلالی، با بیش از ۳۱۲۰ تست round-trip در
`tests/jalali.test.ts`. تابع اصلی نمایش: `formatDateForDisplay()` — نقطهٔ واحدی که همهٔ ماژول‌ها
برای نمایش تاریخ به کاربر استفاده می‌کنند.

### تولید شناسه — `src/renderer/lib/id.ts`
تابع `generateId(prefix)` — تنها راه مجاز برای ساخت ID رکوردهای جدید در کل پروژه (هرگز
`Date.now()` به‌تنهایی، به‌خاطر ریسک واقعی تصادم شناسه که قبلاً رخ داده بود).

### هش رمز عبور — `src/renderer/lib/hash.ts`
SHA-256 دست‌نویس (بدون salt)، استفاده‌شده فقط در `useUsers.ts`.

### جمع‌بندی دوره‌ای حسابداری — `src/renderer/lib/periodAggregation.ts`
`buildPeriodBuckets(transactions, period, referenceDate)` تراکنش‌های غیر-باطل‌شده را در بازه‌های
روزانه (۱۴ سطل)/هفتگی (۸ سطل)/ماهانه (۱۲ سطل)/سالانه (۵ سطل، بر مبنای سال شمسی) دسته‌بندی
می‌کند. `getPeriodRange(period, referenceDate)` همان بازهٔ زمانی دقیق نمودار را برمی‌گرداند تا
بخش «ریز تراکنش‌های بازهٔ انتخابی» با نمودار هماهنگ بماند. برای «بازهٔ دلخواه»، مقایسهٔ
تاریخ‌ها همیشه به‌صورت رشتهٔ `YYYY-MM-DD` (نه شیء `Date`) انجام می‌شود — چون مقایسهٔ رشته‌ای
ISO از رشتهٔ ذخیره‌شده در `CashTransaction.date` (که خودش با `toISOString().slice(0,10)` ساخته
می‌شود) هیچ ریسک اختلاف منطقهٔ زمانی (timezone) ندارد؛ مقایسهٔ دو شیء `Date` ساخته‌شده با روش‌های
متفاوت (یکی محلی، یکی UTC) می‌تواند در مرز روز اشتباه شود.

### محاسبهٔ اقساط
`src/renderer/modules/installments/useInstallments.ts` → `createContract`:
```
باقیمانده_پایه = مبلغ_کل − پیش‌پرداخت
مبلغ_کارمزد   = round(باقیمانده_پایه × درصد_کارمزد / 100)
باقیمانده_نهایی = باقیمانده_پایه + مبلغ_کارمزد
مبلغ_هر_قسط   = باقیمانده_نهایی / تعداد_اقساط
```
درصد کارمزد پیش‌فرض از تنظیمات (`installmentFeePercent`) پیش‌پر می‌شود ولی روی هر قرارداد قابل
تغییر است و همان مقدار (نه مقدار پیش‌فرض فعلی) روی خودِ قرارداد ذخیره می‌ماند. وضعیت «تسویه شده»
بر اساس **مجموع مبلغ پرداخت‌شده در برابر بدهی کل**، نه تعداد پرداخت‌ها، تعیین می‌شود — چون
پرداخت‌ها می‌توانند مبلغ دلخواه در زمان دلخواه باشند.

### زنجیره‌های خودکار بین‌ماژولی (یک ثبت → انتشار خودکار)
هر رویداد زیر بدون دخالت کاربر، به مقصد خودش می‌رسد:
- فروش → درآمد در حسابداری
- خرید (نقدی یا تسویه‌شدهٔ نسیه) از تأمین‌کننده → هزینه در حسابداری
- تحویل تعمیر با اجرت → درآمد در حسابداری
- پیش‌پرداخت و هر قسط پرداختی → درآمد در حسابداری
- ضبط ضمانت → درآمد در حسابداری؛ برگشت از ضبط → هزینهٔ جبرانی (بدون حذف تراکنش قبلی)
- افزودن کالای جدید به انبار (با تیک «ثبت به‌عنوان خرید واقعی»، پیش‌فرض روشن) → هزینهٔ «خرید کالا»
- ثبت خرید در ماژول انبار پیشرفته → افزایش موجودی انبار مقصد + ثبت خرید نزد تأمین‌کننده (بدهی/حسابداری)
- مشتری جدید → صف پیامک خوشامدگویی
- تعمیر تکمیل‌شده → صف پیامک آماده‌شدن دستگاه
- هر تراکنش، فیلد `paymentMethod` (نقد/کارت‌خوان(پوز)/انتقال وجه) را از منشأ خودش به ارث می‌برد

قاعدهٔ ثابت: **هیچ تراکنش مالی هرگز حذف نمی‌شود** — فقط `voided: true` می‌شود (تابع
`voidTransaction`، بدون هیچ `deleteTransaction`).

---

## ۸. فهرست کامل قابلیت‌ها به تفکیک ماژول

### داشبورد (`dashboard`)
کارهای امروز، تعمیرات آمادهٔ تحویل، اقساط نزدیک سررسید، موجودی کم، پیشنهاد محتوای اینستاگرام بر
اساس ساعت (یک برنامهٔ ساعتی ثابت، نه تحلیل واقعی رفتار — بدون دسترسی به آمار اینستاگرام)، مقایسهٔ
فروش امروز/دیروز.

### فروش (`sales`)
ثبت فروش با کسر خودکار موجودی؛ فیلد **نوع فروش** (حضوری/اینستاگرام/تلگرام/واتساپ) و **روش
پرداخت** (نقد/کارت‌خوان (پوز)/انتقال وجه)، هر دو قابل فیلتر در تاریخچهٔ فروش؛ گزینهٔ چاپ خودکار
بعد از فروش (تنظیم‌پذیر)؛ دکمهٔ «چاپ» مستقل روی هر ردیف تاریخچهٔ فروش (بدون نیاز به فروش تازه).

### تعمیرات (`repairs`)
ثبت دستگاه (مدل، IMEI، سریال، رمز/الگو، شرح خرابی، لوازم تحویلی)، اولویت، تکنسین، مشتری، **تاریخ
تحویل تخمینی با Date Picker شمسی کامل** (سال/ماه/روز، نه ورودی تقویم میلادی مرورگر)، **بیعانه**،
**تلفن همراه/ثابت**، **کد ملی** (برای رسید چاپی). امضای دیجیتال مشتری (signature pad) حذف شده —
به‌جایش دو دکمهٔ چاپ مستقل: «رسید تحویلی» (کار انجام‌شده، قطعات تعویض‌شده، اجرت، بیعانه، مبلغ
باقیمانده) و «رسید تعمیر» (فرم مسئولیت قانونی مطابق نمونه، برای امضای دستی با خودکار).

### مشتریان (`customers`)
افزودن/ویرایش/حذف؛ همهٔ فیلدها اختیاری؛ **تاریخ تولد کامل (سال/ماه/روز شمسی)** با Date Picker
شمسی — برای یادآوری سالانهٔ تولد (فقط ماه/روز از تاریخ کامل استخراج و مقایسه می‌شود) و ثبت سن
واقعی مشتری.

### مدیریت کالا (`inventory`)
افزودن (با قیمت خرید و فروش)/ویرایش/حذف؛ دسته‌بندی (موبایل/تبلت/لوازم جانبی/گجت/تعمیراتی)؛ تیک
«ثبت به‌عنوان خرید واقعی» هنگام افزودن کالای جدید (پیش‌فرض روشن) که هزینهٔ خرید را خودکار در
حسابداری ثبت می‌کند.

### قیمت لحظه‌ای (`live-prices`)
اسکرِیپ خودکار (cheerio، در پردازش اصلی) از یک سایت مرجع قابل‌تنظیم، با CSS selector برای
ردیف/نام/قیمت هر کالا، روی یک تایمر قابل‌تنظیم (حداقل ۵ دقیقه).

### انبار پیشرفته (`warehouse`)
چند انبار (هر کدام موجودی مستقل، به‌جز «انبار مرکزی» که همان `inventory` است)؛ انتقال بین
انبارها؛ انبارگردانی؛ **ثبت خرید** (انتخاب تأمین‌کننده → «خرید از» خودکار پر می‌شود → موجودی
افزایش می‌یابد → خرید نزد تأمین‌کننده هم ثبت می‌شود)؛ رزرو کالا برای مشتری (با
مشاهده/ویرایش/حذف/چاپ رسید)؛ کالای معیوب؛ مرجوعی.

### فروش اقساطی (`installments`)
شرکت‌های طرف قرارداد (اختیاری)؛ ثبت پروندهٔ اقساط با: پیش‌پرداخت، شرح پرونده (قابل ویرایش بعد از
ثبت)، شمارهٔ سریال چک، فیلد «ثبت در کارتابل پذیرنده» (بله/خیر)، درصد کارمزد قابل تنظیم؛ پرداخت
مرحله‌ای با هر مبلغ دلخواه در هر زمان؛ نمایش خودکار مانده بدهی و مبلغ پرداخت‌شده؛ تنظیمات: درصد
کارمزد پیش‌فرض، تعداد روز یادآوری پیش از سررسید، و اینکه کدام فیلدها در چاپ نمایش داده شوند.

### مدیریت ضمانت (`collateral`)
انواع: چک، طلا، سفته، ضامن، **اعتباری** (با فیلد ثابت و غیرقابل‌تغییر «بانک ملی»)، سایر؛ نام
ضامن و **نام خریدار** هر دو ثبت و نمایش داده می‌شوند؛ وضعیت معتبر/بازگردانده‌شده/ضبط‌شده؛ بخش
مجزای «ضمانت‌های اجرا شده» (فیلتر خودکار وضعیت ضبط‌شده)؛ هشدار نزدیک سررسید.

### تأمین‌کنندگان (`suppliers`)
ثبت خرید (نقد یا نسیه)، تسویهٔ بدهی، هر دو با انتخاب روش پرداخت؛ رتبه‌بندی تأمین‌کننده.

### حسابداری (`accounting`)
ثبت دستی هزینه/درآمد با دسته‌بندی و روش پرداخت؛ نمودار میله‌ای + جدول برای بازه‌های
روزانه/هفتگی/ماهانه/سالانه؛ **بازهٔ زمانی دلخواه** (دو تاریخ‌گزین)؛ بخش «ریز تراکنش‌های بازهٔ
انتخابی» هماهنگ با بازهٔ فعال؛ جدول کامل همهٔ تراکنش‌ها با فیلتر روش پرداخت؛ ابطال تراکنش (بدون
حذف)؛ مدیریت چک‌های دریافتنی/پرداختنی.

### ماشین‌حساب (`calculator`)
محاسبهٔ سود، تخفیف (درصدی یا مبلغ ثابت — هر دو با `CurrencyInput` هنگام مبلغ ثابت)، مالیات، اقساط،
با قابلیت چاپ مستقیم.

### چاپ (`printing`)
پنج نوع سند: فاکتور فروش (با دکمهٔ چاپ مستقل روی هر ردیف تاریخچهٔ فروش)، **رسید تعمیر** (مطابق
یک نمونهٔ واقعی طراحی شده — عنوان و تلفن از تنظیمات فروشگاه، تاریخ چاپ واقعی لحظهٔ چاپ، جدول
۶ فیلدی، ۸ بند مسئولیت قانونی، بند تعهد نهایی با نام و کد ملی خودکار، خط امضای خالی برای امضای
دستی)، **رسید تحویلی تعمیر** (کار انجام‌شده، قطعات تعویض‌شده، اجرت، بیعانه، مبلغ باقیمانده —
مستقل از رسید تعمیر)، پروندهٔ فروش اقساطی (با فیلدهای قابل‌تنظیم)، و رسید رزرو کالا.

### پیامک/اعلان‌ها (`notifications`)
دریافت شماره از دستگاه ثبت‌شماره؛ صف رویداد پیامکی (تأیید دستی قبل از ارسال — نه ارسال خودکار)؛
یادآوری‌های خودکار تولد/قسط (با تعداد روز قابل‌تنظیم)/ضمانت؛ اتصال به پنل پیامکی عمومی REST؛
مدیریت قالب پیامک.

### امنیت (`security`)
مدیریت کاربران و نقش (مدیر/صندوقدار/تکنسین/انباردار)، لاگ فعالیت با سطح (info/error).

### تنظیمات (`settings`)
پروفایل فروشگاه (نام، لوگو، تلفن)، پرینتر و سایز کاغذ، بکاپ (خودکار واقعی + دستی + فهرست + بازیابی
+ حذف)، به‌روزرسانی خودکار برنامه، مدیریت افزونه‌ها، تنظیمات محاسبات (مالیات پیش‌فرض، چاپ خودکار
بعد از فروش)، تنظیمات فروش اقساطی، تنظیمات اینترنت/AI provider.

---

## ۹. بکاپ واقعی

مسیر: `src/main/backup.ts` + `src/main/backupCore.ts`.
- بکاپ دستی («گرفتن بکاپ الان») و خودکار (بازهٔ ساعتی قابل‌تنظیم، در تنظیمات) هر دو واقعی هستند —
  یک فایل واقعی روی دیسک کاربر (پوشهٔ قابل‌انتخاب) نوشته می‌شود.
- فهرست بکاپ‌ها، بازیابی از یک فایل بکاپ، و حذف یک فایل بکاپ همگی از تنظیمات در دسترس‌اند.

## ۱۰. به‌روزرسانی خودکار

مسیر: `src/main/update.ts` (با `electron-updater`، در برابر GitHub Releases).
- نیازمند این‌که `package.json` → `repository.url` به آدرس واقعی مخزن GitHub تنظیم شود؛ تا
  زمانی‌که تنظیم نشده، فقط خطای واضح می‌دهد (بدون کرش).
- انتشار نسخهٔ جدید فقط با push کردن یک تگ نسخه (`v*`) به گیت‌هاب فعال می‌شود — روال معمول
  build (push به شاخهٔ اصلی) کاملاً بدون تغییر باقی مانده و هرگز منتشر نمی‌کند.
- بازگشت به نسخهٔ قبلی به‌صورت خودکار پشتیبانی نمی‌شود (محدودیت فنی NSIS/electron-updater)؛
  دکمهٔ مربوطه فقط صفحهٔ GitHub Releases را برای دانلود دستی نسخهٔ قدیمی باز می‌کند.

## ۱۱. CI/CD

مسیر: `.github/workflows/build.yml`.
- Job اول (`build-windows`): روی هر push به شاخهٔ اصلی، هر Pull Request، و هر تگ اجرا می‌شود —
  نصب وابستگی‌ها → typecheck → تست واحد → build → package → آپلود artifact قابل دانلود دستی.
- Job دوم (`publish-release`): فقط روی تگ نسخه (`v*`) اجرا می‌شود — همان build را می‌سازد و به‌جای
  artifact ساده، آن را به‌عنوان یک GitHub Release منتشر می‌کند (برای فعال شدن به‌روزرسانی
  خودکار کاربران).
- تنظیمات رم: `NODE_OPTIONS=--max-old-space-size=4096`، فقط ویندوز (بدون matrix چندپلتفرمی)،
  مراحل پشت‌سرهم (نه موازی)، کش `node_modules` و کش باینری‌های electron-builder.

---

## ۱۲. قواعد ثابت پروژه (باید همیشه رعایت شوند)

1. شناسهٔ رکورد جدید همیشه با `generateId(prefix)`، هرگز `Date.now()` تنها.
2. تاریخ‌ها همیشه با `formatDateForDisplay()` نمایش داده شوند.
3. مبالغ همیشه با کامپوننت `CurrencyInput` وارد شوند.
4. تراکنش مالی هرگز حذف نمی‌شود، فقط `voided`.
5. بدون npm dependency جدید مگر واقعاً ضروری و سبک (به‌خاطر محدودیت رم CI).
6. importهای نسبی در `src/main/` باید پسوند `.js` صریح داشته باشند (ESM واقعی).
7. شمارهٔ تلفن همه‌جا محدود به ۱۱ رقم.
8. بعد از هر تغییر، `npm run typecheck` باید بدون خطا باشد؛ برای تغییرات منطقی غیربدیهی، تست
   Vitest نوشته شود.
9. هیچ فیلد ورودی فرم نباید متن راهنمای «مثال» (placeholder نمایشی) داشته باشد — فقط برچسب
   (`label`) دقیق کافی است.
10. هیچ کامنت یا مستندی نباید به فرآیند توسعه، گفتگو، یا ابزار نوشتن کد اشاره کند — فقط توضیح
    فنی خالص و بی‌طرف.

---

## ۱۳. آنچه هنوز پیاده‌سازی نشده (برای شروع کار بعدی)

- قابلیت‌های واقعی هوش مصنوعی (تحلیل فروش/سود/مشتری، OCR، ترجمه) — بخش تنظیمات AI provider فقط
  پیکربندی را نگه می‌دارد، هنوز به هیچ قابلیت واقعی وصل نیست.
- حالت آفلاین واقعی و همگام‌سازی چند‌دستگاهی/چند‌شعبه — به‌صراحت از دامنهٔ فعلی کنار گذاشته شده
  (این اپ تک‌دستگاهی است، بدون سرور/کلاود).
- بارکد/QR Code واقعی.
- تأیید نهایی زندهٔ یک مورد: رفتار گزارش سالانهٔ حسابداری در بازهٔ مرزی سال — کد بازبینی و
  منطقاً درست تأیید شده، ولی تأیید در محیط اجرای واقعی (نه این محیط توسعه) هنوز انجام نشده.

---

## ۱۴. تاریخچهٔ تغییرات

هر تغییر جدید باید یک خط به انتهای این فهرست اضافه کند (خلاصهٔ تغییر و فایل‌های اصلی درگیر).

- پایه: معماری Plugin-Based + ماژول‌های اصلی (داشبورد، کالا، فروش، مشتریان، تعمیرات، انبار، اقساط،
  ضمانت، تأمین‌کنندگان، حسابداری، ماشین‌حساب، چاپ، پیامک، امنیت، تنظیمات).
- اتصال کامل اقساط و ضمانت به حسابداری؛ ثبت خودکار هزینهٔ خرید کالای جدید (اختیاری، با تیک).
- تنظیمات فروش اقساطی: درصد کارمزد، روز یادآوری پیامکی، فیلدهای قابل‌چاپ.
- **رفع مشکل بنیادی نبود پایگاه‌داده واقعی**: افزودن persistKey به تمام ۱۵ استور دادهٔ کسب‌وکار
  در `createStore.ts` — همهٔ داده‌ها اکنون روی دیسک ذخیره می‌مانند.
- نوع فروش (حضوری/اینستاگرام/تلگرام/واتساپ) و روش پرداخت (نقد/کارت‌خوان/انتقال وجه) در سراسر
  برنامه.
- فروش اقساطی کامل‌تر: ویرایش شرح پرونده، شمارهٔ سریال چک، فیلد کارتابل پذیرنده، نمایش خودکار
  مانده بدهی.
- انبار پیشرفته: بخش «ثبت خرید» با تکمیل خودکار «خرید از» و اتصال به تأمین‌کننده.
- گزارش درآمد: بازهٔ زمانی دلخواه + ریز تراکنش‌های بازهٔ انتخابی؛ رفع باگ کلاس مقایسهٔ
  timezone در فیلتر تاریخ.
- به‌روزرسانی خودکار برنامه (electron-updater + GitHub Releases، انتشار فقط با تگ نسخه).
- اعمال واقعی محدودیت دسترسی نقش‌ها (RBAC): گیت ورود واقعی + فیلتر نوار کناری بر اساس نقش.
- مدیریت افزونه‌ها (فعال/غیرفعال + مستند SDK) به‌جای فروشگاه افزونهٔ زندهٔ ناامن.
- بازبینی کامل پروژه: رفع ۴ باگ کوچک (یک فیلد بدون `CurrencyInput`، سه شناسهٔ ساخته‌شده با
  `Date.now()` به‌جای `generateId`).
- رسید تعمیر بازطراحی‌شده مطابق نمونهٔ واقعی؛ افزودن فیلدهای بیعانه/تلفن همراه/تلفن ثابت/کد ملی
  به پروندهٔ تعمیر؛ رفع باگ تاریخ چاپ (نمایش لحظهٔ واقعی چاپ).
- مدیریت رزرو انبار: مشاهده/ویرایش/حذف/چاپ رسید.
- رفع باگ واقعی در `createStore.ts`: تغییراتی که بلافاصله بعد از ساخت استور (پیش از پایان بارگذاری
  اولیه) رخ می‌دادند هرگز ذخیره نمی‌شدند؛ اصلاح شد تا نوشتن همیشه زمان‌بندی شود و مقدار لحظهٔ
  واقعی اجرا را ذخیره کند.
- رفع کرش کامل برنامه هنگام اجرا: import نادرست `electron-updater` (پکیج CommonJS با import
  اسمی به‌جای پیش‌فرض).
- پاک‌سازی کامل مستندات و کامنت‌های غیرفنی از سراسر پروژه؛ ایجاد این فایل (`reza.md`) به‌عنوان
  مرجع کامل و مستقل پروژه.
- دکمهٔ چاپ مستقل روی هر ردیف تاریخچهٔ فروش. تاریخ تولد مشتری از فقط ماه/روز به سال/ماه/روز
  کامل تغییر کرد (کامپوننت جدید `JalaliDatePicker`، جایگزین `JalaliMonthDayPicker` قدیمی).
  در ماژول تعمیرات: تاریخ تحویل تخمینی با همان `JalaliDatePicker` جایگزین ورودی تقویم میلادی
  شد؛ امضای دیجیتال مشتری (signature pad) کامل حذف شد و به‌جایش دو نوع چاپ مستقل اضافه شد:
  «رسید تحویلی» (کامپوننت جدید `RepairDeliveryBody`، شامل قطعات/اجرت/بیعانه/مانده) و «رسید
  تعمیر» (بدون تغییر). `printRequestStore` برای پشتیبانی از دو نوع سند تعمیر گسترش یافت.
